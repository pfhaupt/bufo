//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

func foo() -> bool {
    return false;
}

func main() -> i32 {
    let a: bool = false;
    let b: bool = a != a;
    let c: bool = a == a;
    if (b) return 1;
    // FIXME: We need Unary Not :^)
    if (c) {
        if (foo()) return 1;
        return 0;
    } else {
        return 1;
    }
}