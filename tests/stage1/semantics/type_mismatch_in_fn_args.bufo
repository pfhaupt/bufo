//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Could not find matching signature for call to function `foo`.
//! Could not find matching signature for call to function `bar`.

func foo(a: u64) {
}

func bar(a: i32) {
}

func bar(a: u8) {
}

func main() {
    let a: i8 = 0;
    foo(a);
    let b: u32 = 0;
    bar(b);
}

