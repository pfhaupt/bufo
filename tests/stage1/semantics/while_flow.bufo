//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

import "prelude.bufo";

func main(argc: i32, argv: &&char) -> i32 {
    let a: u64 = 0;
    while (a < 10) {
        a = a + 1;
    }
    assert(a == 10);
    return 0;
}
