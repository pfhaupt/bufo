//! THIS IS A TEST PROGRAM
//! STAGE: 1
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Use of undeclared variable `a`.
//! Use of undeclared variable `b`.
//! Use of undeclared variable `c`.
//! Use of undeclared variable `d`.

func main()->i32 {
    mut e: u32 = 10;
    e = a;
    if (b + (d + d + d + (d + d)) == a) {

    }
    while (c) {
        
    }
    return d;
}
