//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Type mismatch in binary expression! Operation `i32 == u32` is not defined.
//! LHS has type `i32`.
//! RHS has type `u32`.

func main() {
    let a: i32 = 0;
    let b: u32 = 0;
    if (a == b) {
        let c: i32 = a;
    }
}