//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Operation `*` is not allowed in the context of pointer arithmetics.

func main() -> i32 {
    let a: i32 = 5;
    let b: &i32 = &a;
    *(5 * b) = 10;
    if (a != 10) return 1;
    if (a != 20) return 1;
    return 0;
}
