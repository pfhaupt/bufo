//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

import "prelude.bufo";
import "libc.bufo";

func main(argc: i32, argv: &&char) -> i32 {
    let s1: &char = "Hello, how are you?";
    assert(strlen(s1)== 19);
    let s2: &char = "I am fine, thanks for asking!";
    assert(strlen(s2) == 29);
    return 0;
}

