//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

import "prelude.bufo";

func main(argc: i32, argv: &&char) -> i32 {
    mut ctr: usize = 0;
    mut i: usize = 0;
    while (i < 10) {
        i = i + 1;
        if (i == 5) {
            continue;
        }
        ctr = ctr + 1;
    }
    assert(ctr == 9);
    return 0;
}
