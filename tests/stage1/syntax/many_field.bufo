//! THIS IS A TEST PROGRAM
//! STAGE: 1
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Field redeclaration.
//! Field `a` already declared here.
//! Field redeclaration.
//! Field `b` already declared here.
//! Field redeclaration.
//! Field `c` already declared here.
//! Method redeclaration.
//! Method `foo` already declared here.
//! Method redeclaration.
//! Method `bar` already declared here.

struct Foo {
    a: i32;
    b: i32;
    c: i32;
    d: i32;
    a: i32;
    b: u32;
    c: u32;
    d: u32;

    func foo() {}
    func foo() {}
    func bar() {}
    func bar() {}
}