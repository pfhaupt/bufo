//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Type mismatch in binary expression! Operation `bool == u32` is not defined.
//! LHS has type `bool`.
//! RHS has type `u32`.

func main() -> u32 {
    let a: u32 = 2;
    let b: u32 = 3;
    let c: u32 = 4;
    if (a == b == c) return 1;
    return 0;
}