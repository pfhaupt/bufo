//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

func main() -> i32 {
    mut sum: u32 = 0;
    let arr: [u32; 5] = [12, 14, 16, 18, 20];
    for (mut i: usize = 0; i < 5; i = i + 1) {
        sum = sum + arr[i];
    }
    if (sum != 80) return 1;
    return 0;
}