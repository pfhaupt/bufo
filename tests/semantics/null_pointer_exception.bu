class Test {
    tField: Test;
    otherField: u32;

    constructor() {
        this.otherField = 10;
    }
}

func main() {
    let t: Test = Test();
    let a: u32 = t.tField.otherField; // should fail because tField was never initialized
}