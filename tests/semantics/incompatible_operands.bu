//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! incompatible_operands.bu:21:23: Type mismatch in binary expression! Operation `Test + Test` is not defined.
//! incompatible_operands.bu:21:20: LHS has type `Test`.
//! incompatible_operands.bu:21:25: RHS has type `Test`.

class Test {
    a: i64;

    constructor() {

    }
}

func main() {
    let t1: Test = Test();
    let t2: Test = Test();
    let t3: Test = t1 + t2; // OP_PLUS is not defined for class Test
}