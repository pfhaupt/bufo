//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Type mismatch in binary expression! Operation `incompatible_operands::Test + incompatible_operands::Test` is not defined.
//! LHS has type `incompatible_operands::Test`.
//! RHS has type `incompatible_operands::Test`.

// FIXME: Should errors always print the fully specified type?

struct Test {
    a: i64;
}

func main() {
    let t1: Test = Test {a: 0};
    let t2: Test = Test {a: 0};
    let t3: Test = t1 + t2; // OP_PLUS is not defined for struct Test
}