//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

struct A {
    a: i32;
    func new() -> A {
        return A{
            a: 0
        };
    }

    func setA(&mut this, a: i32) {
        this.a = a;
    }
}

func main() -> i32 {
    // Note: Won't work if you don't specify the module the struct belongs to
    mut a: A = static::A::new();
    static::A::setA(&mut a, 10);
    if (a.a != 10) return 1;
    return 0;
}
