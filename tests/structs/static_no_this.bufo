//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Not enough arguments for Function `setA`.
//! Function `setA` expects 2 arguments, found 1.

struct A {
    a: i32;
    func setA(&mut this, a: i32) {
        this.a = 10;
    }
}

func main() {
    mut a: A = A {
        a: 0
    };
    static_no_this::A::setA(10);
}