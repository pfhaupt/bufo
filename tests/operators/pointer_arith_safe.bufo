//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

func main() -> i32 {
    mut a: i32 = 5;
    let b: &mut i32 = &mut a;
    unsafe {
        *(b + 5 - 5) = 10;
    }
    if (a != 10) return 1;
    unsafe {
        *(5 + b - 5) = 20;
    }
    if (a != 20) return 1;
    return 0;
}
