//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Type mismatch! Expected type `u64`, found type `i32`.
//! Type mismatch! Negation is not defined for type `u64`.
//! Type mismatch! Expected type `i64`, found type `u64`.
//! Type mismatch! Negation is not defined for type `u32`.

func main() -> u32 {
    let a: i32 = 9;
    let b: i32 = -a;
    let c: u64 = -(a + b);
    let d: i64 = -(c);
    return -5;
}