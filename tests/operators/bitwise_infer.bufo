//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

import "prelude.bufo";

func main(argc: i32, argv: &&char) -> i32 {
    let a: i32 = 5 & 3;
    assert(a == 1);
    let b: u8 = 7 | (a as u8);
    assert(b == 7);
    let c = b ^ 3;
    assert(c == 4);
    return 0;
}
