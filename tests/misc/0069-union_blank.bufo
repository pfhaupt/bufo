//! THIS IS A TEST PROGRAM
//! COMPILER
//! DIAGNOSTICS
//! WARNING:
//! Using `blank` to initialize value of type A.
//! Using `blank` to initialize value of type B.

union A {
    A,
    B,
    C(i32),
}

union B {
}

func main(argc: i32, argv: &&char) -> i32 {
    let a: A = blank;
    let b: B = blank;
    return 0;
}
