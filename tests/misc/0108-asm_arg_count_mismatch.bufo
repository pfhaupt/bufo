//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! The function passed to `asm` expected 0 parameters, but got 1 argument.

import "asm.bufo";

func generate() -> ASMContext {
}

func main(argc: i32, argv: &&char) -> i32 {
    asm(generate, argc);
    return 0;
}
