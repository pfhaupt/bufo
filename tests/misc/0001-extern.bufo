//! THIS IS A TEST PROGRAM
//! RUNTIME
//! FAILURE
//! CODE: 5
//! ERROR:

// NOTE: Only fails because we call exit(5) :^)

import "libc.bufo";

let p = C::printf;
let e = C::exit;

func main(argc: i32, argv: &&char) -> i32 {
    p("Hello World!\n");
    p("%u\n", argc);
    e(5);
    return 0;
}
