//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

import "prelude.bufo";

struct Foo {
    dummy: u8; // Suppress `blank of type of size 0` warning
    func get(&this, a: i32) -> i32 {
        return a;
    }
    func get(&this, a: usize) -> usize {
        return a;
    }
}

func main(argc: i32, argv: &&char) -> i32 {
    let f: Foo = blank;
    let i: i32 = 2;
    let j: usize = 3;
    assert(f.get(i) == 2);
    assert(f.get(j) == 3);
    return 0;
}
