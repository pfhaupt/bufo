//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Type mismatch in binary expression! Operation `char / char` is not defined.
//! LHS has type char.
//! RHS has type char.
//! Type mismatch in binary expression! Operation `char * char` is not defined.
//! LHS has type char.
//! RHS has type char.

func main(argc: i32, argv: &&char) -> i32 {
    let a = 'a';
    let b = 'b';
    let c = a / b;
    let d = a * b;
    return 0;
}
