//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Patterns of a match statement must be exhaustive.
//! You can use the wildcard pattern `_` to catch all other cases.

import "prelude.bufo";

func main(argc: i32, argv: &&char) -> i32 {
    let a: i32 = 12;
    match (a) {
        12 => {
            assert(true);
        }
    }
    return 0;
}
