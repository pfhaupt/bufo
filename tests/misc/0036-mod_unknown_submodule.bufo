//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Module `foo` does not contain any submodules named `bar`.
//! Module declared here.

module foo {
    struct Foo {
        a: i32;
    }
}

func f(f: foo::bar::Foo) -> foo::Foo {
    return f;
}

func main(argc: i32, argv: &&char) -> i32 {
    return 0;
}
