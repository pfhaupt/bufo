//! THIS IS A TEST PROGRAM
//! COMPILER
//! DIAGNOSTICS
//! WARNING:
//! Array is zero-sized and can never be indexed into.

import "prelude.bufo";

func main(argc: i32, argv: &&char) -> i32 {
    let a: [i32; 0] = [];
    a[0]; // <- Will always panic and crash
    return 0;
}
