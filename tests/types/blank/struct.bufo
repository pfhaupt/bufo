//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

struct Foo {
    field: usize;
    value: usize;
    ptr: Any;
}

func main() {
    let f: Foo = blank;
    assert(f.field == 0);
    assert(f.value == 0);
    unsafe {
        assert(isNull(f.ptr));
    }
}
