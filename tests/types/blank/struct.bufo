//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

import "prelude.bufo";

struct Foo {
    field: usize;
    value: usize;
    ptr: Any;
}

func main(argc: i32, argv: &&char) -> i32 {
    let f: Foo = blank;
    assert(f.field == 0);
    assert(f.value == 0);
    assert(f.ptr == null);

    return 0;
}
