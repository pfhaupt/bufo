//! THIS IS A TEST PROGRAM
//! RUNTIME
//! SUCCESS

import "prelude.bufo";

func main(argc: i32, argv: &&char) -> i32 {
    let a: usize = blank;
    assert(a == 0);

    let b: i8 = blank;
    assert(b == 0);

    let c: u16 = blank;
    assert(c == 0);
    
    let d: i32 = blank;
    assert(d == 0);

    return 0;
}
