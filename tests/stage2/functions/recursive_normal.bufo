//! THIS IS A TEST PROGRAM
//! STAGE: 2
//! RUNTIME
//! SUCCESS

let i32 = @integer(32);
let i64 = @integer(64);

let none = @none();

let a = func (e: *mut i32, ctr: i64) {
    if ctr == 10 {
        return;
    }
    a(e, ctr + 1);
    *e = 10;
};

let abc = func () {};
let main = func () -> i32 {
    mut v = 0;
    a(&mut v, 0);
    if v != 10 {
        return 1;
    }
    return 0;
};

@entry(main);

