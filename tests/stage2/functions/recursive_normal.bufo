//! THIS IS A TEST PROGRAM
//! STAGE: 2
//! RUNTIME
//! SUCCESS

let a = func (a: *mut i32, ctr: u64) {
    if ctr == 10 return;
    a(e, ctr + 1);
    *e = 10;
};

let main = func () -> i32 {
    mut v: i32 = 0;
    a(&mut v, 0);
    if v != 10 return 1;
    return 0;
};

@entry(main);
