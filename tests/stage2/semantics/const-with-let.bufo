//! THIS IS A TEST PROGRAM
//! STAGE: 2
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! error: Variable `a` is used in a `const` context, however it only exists at runtime.
//! note: Variable declared here.

const none = @none();

const main = func () {
    let a = 1;
    const c = a;
};

@entry(main);
