//! THIS IS A TEST PROGRAM
//! STAGE: 2
//! RUNTIME
//! SUCCESS

const false = @boolean()(0);
const u32 = @integer(32, false);

const main = func () -> u32 {
    let a = 1;
    if a == 1 {
        return 0;
    } else {
        return 1;
    }
};

@entry(main);
