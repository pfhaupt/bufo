//! THIS IS A TEST PROGRAM
//! STAGE: 2
//! COMPILER
//! SUCCESS

const true = 1 == 1;
const i32 = @integer(32, true);

const foo = func (t: i32) -> i32 {
    // NOTE: Because we never call foo(), we never type check the body of it
    const a = i33; // <-- Error is ignored
};

const none = @none();
// bar has implicit return type none
const bar = func () {
};

@check(foo);
@check(bar);

