//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Expected one of `class` or `func`, found `}`
//! Expected Expression, found `}`
//! Expected one of `class` or `func`, found `}`
//! Expected one of `class` or `func`, found `while`
//! Expected one of `class` or `func`, found `}`
//! Expected one of `class` or `func`, found `}`

class Foo {

}
}
}

func main() {{{
    if (1 ==1 ) }}}}}}
    while (1 = 1) {{}}}
}

func main2() {}
}