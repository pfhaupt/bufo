//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Expected one of `func`, `extern` or `struct`, found `}`
//! Expected Expression, found `}`
//! Expected one of `func`, `extern` or `struct`, found `}`
//! Expected one of `func`, `extern` or `struct`, found `while`
//! Expected one of `func`, `extern` or `struct`, found `}`
//! Expected one of `func`, `extern` or `struct`, found `}`

struct Foo {

}
}
}

func main() {{{
    if (1 ==1 ) }}}}}}
    while (1 = 1) {{}}}
}

func main2() {}
}