//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Expected one of `extern`, `func`, `import`, `struct` or `unsafe`, found `}`
//! Expected Expression, found `}`
//! Expected one of `extern`, `func`, `import`, `struct` or `unsafe`, found `}`
//! Expected one of `extern`, `func`, `import`, `struct` or `unsafe`, found `while`
//! Expected one of `extern`, `func`, `import`, `struct` or `unsafe`, found `}`
//! Expected one of `extern`, `func`, `import`, `struct` or `unsafe`, found `}`

struct Foo {

}
}
}

func main() {{{
    if (1 ==1 ) }}}}}}
    while (1 = 1) {{}}}
}

func main2() {}
}