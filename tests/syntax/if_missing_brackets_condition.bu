//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Expected `(`, found Identifier
//! Expected one of `class` or `func`, found `}`

func main() {
    if a == 1 {
        EXIT(1);
    }
}