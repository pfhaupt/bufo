//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Expected `(`, found Identifier
//! Expected one of `func`, `extern` or `struct`, found `}`

func main() {
    while a == 1 {
        EXIT(1);
    }
}