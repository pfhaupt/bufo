//! THIS IS A TEST PROGRAM
//! COMPILER
//! FAILURE
//! CODE: 1
//! ERROR:
//! Expected Top Level Keyword, found `}` instead.
//! A non-exhaustive list of TLK: `import`, `let`, `comptime`, `struct`, `union`, `func`.

struct Foo {

}
} // <-- here
}

func main() {{{
    if (1 ==1 ) }}}}}}
    while (1 = 1) {{}}}
}

func main2() {}
}
