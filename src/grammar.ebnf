File: Fn*;

Fn = "func" "name" "(" ")" "{" Block "}";

Block = (Stmt*);

Stmt = StmtExpr | StmtLet;

StmtExpr = Expr ";";
StmtLet = "let" "name" "=" Expr ";";

Expr = ExprName | ExprLiteral;
ExprName = "name";
ExprLiteral = "int";