File: Fn*;

Fn = "func" "name" ParamList Block;

Param = "name" [","];
ParamList = "(" {Param} ")";

Block = "{" {Stmt} "}";

Stmt = StmtExpr
     | StmtLet
     | StmtAssign
     | StmtCall
     | StmtIf;

StmtExpr = Expr ";";
StmtLet = "let" "name" "=" Expr ";";
StmtAssign = "name" "=" Expr ";";
StmtCall = "name" ArgList ";";
StmtIf = "if" "(" ExprCmp ")" Block {"else" Block};

Arg = Expr [","];
ArgList = "(" {Arg} ")";

Expr = ExprName
     | ExprLiteral
     | ExprBinary
     | ExprParen;
ExprName = "name";
ExprLiteral = "int";
ExprBinary = Expr ("+" | "-" | "*" | "/" | "==" | "!=" | "<" | ">" | "<=" | ">=") Expr;
ExprParen = "(" Expr ")";