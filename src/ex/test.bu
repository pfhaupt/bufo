func ack(m: u32, n: u32) -> u32 {
    if (m == 0) { return n + 1; }
    if (n == 0) { return ack(m - 1, 1); }
    return ack(m - 1, ack(m, n - 1));
}

func main() {
    let a: u32 = ack(3, 5);
    let b: u32 = (1u32 + 1u32) == 4i32;
}