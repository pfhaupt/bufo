func factorial(n, acc) {
    let c = acc;
    if (n > 0) {
        factorial(n - 1, c * n);
        let d = 5;
    } else {
        let c = 42069;
    }
    c = 1337;
}

func main() {
    let a = 10;
    factorial(a, 1);
    let b = a;
}