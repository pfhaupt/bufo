func test(acc: u32, n: &u32) {
    let b: &u32 = &acc;
    if (acc < 4) {
        *n = acc;
        test(acc + 1, n);
    }
}

func main() {
    let a: u32 = 4;
    test(0, &a);
    let c: u32 = a;
    let e: &u32 = &c;
}