func factorial(n, acc) {
    if (n > 0) {
        let c = acc;
        factorial(n - 1, acc * n);
    } else {
        let c = 42069;
    }
}

func main() {
    let a = 10;
    factorial(a, 1);
    let b = a;
}