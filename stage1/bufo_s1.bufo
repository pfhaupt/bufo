import parser;
import string;

func main() {
    mut s: String = string::String::new();
    for (mut i: usize = 0; i < 100; i = i + 1) {
        s.push_str("Hello there!");
    }
    unsafe {
        printf("[DEBUG] String after pushing: `%s`\n", s.as_char_ptr());
    }
}