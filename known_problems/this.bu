class Foo {
    feat new() {
        let this: i32 = 0; // Should be an error
    }
    func other() {
        let this: i32 = 0; // What the heck, why does this not error?
    }
    func other2(a: i32) {
        let a: i32 = 0; // Should be a warning
    }
}

func main() {
    let foo: Foo = Foo();
}