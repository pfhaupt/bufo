let i32 = @integer(32);
let i64 = @integer(64);
let f32 = @float(32);

let Vector2 = struct {
    x: f32;
    y: f32;
};

let Cube = struct {
    r1: Rect;
    r2: Rect;
    r3: Rect;
    r4: Rect;
    r5: Rect;
    r6: Rect;
};
let Line = struct {
    start: Point;
    end: Point;
};
let Rect = struct {
    topRight: Point;
    bottomLeft: Point;
    diagonal: Line;
};

let Point = struct {
    x: i32;
    y: i32;

    let Foo = struct {
        abc: i32;
    };
};
